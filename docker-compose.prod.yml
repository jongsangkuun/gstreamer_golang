services:
  api:
    environment:
      DEBUG: ${PROD_DEBUG}
      GST_DEBUG_LEVEL: ${PROD_GST_DEBUG_LEVEL}
      HLS_OUTPUT: ${PROD_HLS_OUTPUT}
      SQLITE_DB_PATH: ${DEV_SQLITE_DB_PATH}
      FILE_SERVER_HOST: ${FILE_SERVER_HOST}
      FILE_SERVER_PORT: ${FILE_SERVER_PORT}

  file_server:
    environment:
      DEBUG: ${PROD_DEBUG}

